# Copy this file to .env and fill in your real values.
# Never commit the real .env file — it should stay out of git (see .gitignore).

# ------------------------------------------------------------------
# Database
# ------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/alexa_ai"

# ------------------------------------------------------------------
# Server
# ------------------------------------------------------------------
PORT=4000

# ------------------------------------------------------------------
# Authentication
# ------------------------------------------------------------------
# Use a long, random string — this signs and verifies login sessions.
JWT_SECRET="change_this_to_a_long_random_secret"

# ------------------------------------------------------------------
# AI (Groq)
# ------------------------------------------------------------------
GROQ_API_KEY="your_groq_api_key_here"

# ------------------------------------------------------------------
# Email (Resend) — used for password reset emails
# ------------------------------------------------------------------
RESEND_API_KEY="your_resend_api_key_here"

# ------------------------------------------------------------------
# Frontend URL — used to build links inside emails (e.g. password reset)
# ------------------------------------------------------------------
FRONTEND_URL="https://alexa-ai-1.netlify.app"

# ------------------------------------------------------------------
# Payments (Paddle) — optional, only needed if billing is enabled
# ------------------------------------------------------------------
PADDLE_CLIENT_TOKEN="your_paddle_client_token"
PADDLE_PRICE_ID_PRO="your_paddle_price_id"
PADDLE_WEBHOOK_SECRET="your_paddle_webhook_secret"
PADDLE_ENV="sandbox"